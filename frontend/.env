# API Base URL
# Khi chạy từ browser (localhost), frontend sẽ gọi đến localhost:8080
REACT_APP_API_URL=http://localhost:8080

# Note: URL này sẽ được override bởi docker-compose khi chạy trong container