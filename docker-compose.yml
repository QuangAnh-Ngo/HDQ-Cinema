services:
  backend:
    build: ./backend
    container_name: hdq-cinema-backend
    restart: on-failure
    expose:
      - "8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres-db:5432/${POSTGRES_DB}
      SPRING_DATASOURCE_USERNAME: ${POSTGRES_USER}
      SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD}
      JWT_SIGNER_KEY: ${JWT_SIGNER_KEY}
    depends_on:
      - postgres-db
    networks:
      - cinema-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: hdq-cinema-frontend
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf    # <-- mount host default.conf
      - ./frontend/dist:/usr/share/nginx/html                  # <-- mount dist nếu muốn
    depends_on:
      - backend
    networks:
      - cinema-network

  postgres-db:
    image: postgres:17
    container_name: hdq-cinema-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - cinema-network

volumes:
  postgres_data:

networks:
  cinema-network:
    driver: bridge
